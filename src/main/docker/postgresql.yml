version: '2'
services:
  gallery-postgresql:
    image: postgres:12.1
    # volumes:
    #     - ~/volumes/jhipster/gallery/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=gallery
      - POSTGRES_PASSWORD=
    ports:
      - 5432:5432
